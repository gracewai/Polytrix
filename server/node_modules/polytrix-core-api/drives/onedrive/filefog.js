var filefog = require('filefog');
var config = require('../config').onedrive;

filefog.use('onedrive',require('filefog-onedrive'),{
	client_key : config.client_key,
	client_secret : config.client_secret,
	scope: config.scope,
	redirect_url: config.redirect_url
});

module.exports.provider = filefog.provider('onedrive');
module.exports.client = function(access_token, refresh_token){
	return filefog.client('onedrive',{
		access_token: access_token,
		refresh_token: refresh_token
	});
};