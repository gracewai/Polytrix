var Q = require('q');
var req = require('request');
var authenticator = require('../authenticator');

module.exports.accountInfo = function(access_token){
	var url = config.domain + '/vault/'
	return authenticator('GET', access_token, url)
	.then(function (info) {
		return info;
	});
};