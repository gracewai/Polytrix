var auth = require('./auth');
var info = require('./info');
var metadata = require('./metadata');
var operation = require('./operation');
var share = require('./share');
var stream = require('./stream');

module.exports = {
	rootIdentifier: '/',

	//auth
	auth: auth,
	info: info,
	metadata: metadata,
	operation: operation,
	stream: stream,
	share: share,
	
	//deprecated methods name
	getAuthURL: auth.authUrl,
	getToken: auth.getToken,
	renewToken: auth.renewToken,
	getFileIndex: metadata.listFile,
	downloadFileLink: metadata.downloadUrl,
	downloadFilePipe: stream.downloadFilePipe,
	//deprecated methods
	getFolderInformation: getFolderInformation,
	downloadFile: downloadFile,
};